/**
 * @description 這是一個會回傳兩個數字總和的函式
 *
 * @example
 * 返回 2
 * sum(1,1)
 *
 * @example
 * 返回 5
 * sum(2,3)
 *
 * @example
 * 返回 0.6
 * sum(0.2,0.4)
 *
 * @param {number} a - 第一個加數
 * @param {number} b - 第二個家數
 *
 * @return {number} 兩個數的和
 */
function sum(a, b) {
  // TODO: 完成這個 function
}

export default sum;
