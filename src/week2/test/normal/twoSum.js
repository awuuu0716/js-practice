/**
 * @description
 * 輸入一個不重複的正整數陣列 nums, 以及一個目標數字 target, 會返回一個包含兩個 index 的陣列,
 * 透過 index 查詢 nums 的值加總要等於 target
 *
 * 例如 nums 為 [1,2,3], target 為 3,
 * nums[0] 為 1, nums[1] 為 2, 相加後等於 target 3
 * 所以返回 [0,1]
 *
 * 此外, 如果在 nums 中找不到任何兩數相加等於 target, 返回一個空陣列 []
 *
 * @example
 * twoSum([1,2,3,4], 3)
 * 返回 [0,1]
 *
 * @example
 * twoSum([2,6,7], 13)
 * 返回 [1,2]
 *
 * @example
 * twoSum([1,0,1,7], 4)
 * 返回 []
 *
 * @param {number[]} nums
 * @param {number} target
 *
 * @return {number[]}
 */

function twoSum(nums, target) {
  // TODO: 完成這個 function
}

export default twoSum;
