/**
 * @description
 * 輸入一個不重複的整數陣列 nums, 會返回一個最小缺少的正整數
 *
 * @constraints
 *  1 <= nums.length <= Math.pow(10,5)
 *  Math.pow(-2,31) <= nums[i] <= Math.pow(2,31) - 1
 *
 * @example
 * firstMissingPositive([1,2,0])
 * 返回 3
 * 1 跟 2 都有在陣列中, 所以返回 3
 *
 * @example
 * firstMissingPositive([3,4,-1,1])
 * 返回 2
 * 1 有在陣列中, 但是沒有 2, 返回 2
 *
 * @example
 * firstMissingPositive([7,8,9,11,12])
 * 返回 1
 * 最小的正整數 1 不在陣列中, 返回 1
 *
 * @param {number[]} nums
 *
 * @return {number}
 */

function firstMissingPositive(nums) {
  // TODO: 完成這個 function
}

export default firstMissingPositive;
